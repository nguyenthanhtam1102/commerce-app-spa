/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}"],
    theme: {
        container: {
            center: true,
            padding: {
                DEFAULT: '2rem',
                // sm: '5rem',
                // lg: '4rem',
                // xl: '5rem',
                // '2xl': '6rem',
            }
        },
        extend: {
            
        },
    },
    plugins: [],
};
